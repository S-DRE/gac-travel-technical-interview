{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <h1>Categories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ category.createdAt ? category.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class = "btn btn-dark" href="{{ path('app_categories_index') }}"><i class = "fa fa-arrow-left"></i> Volver al índice</a>

    <a class = "btn btn-primary" href="{{ path('app_categories_edit', {'id': category.id}) }}">Editar</a>

    {{ include('categories/_delete_form.html.twig') }}
{% endblock %}
