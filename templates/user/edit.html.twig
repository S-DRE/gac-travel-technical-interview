{% extends 'base.html.twig' %}

{% block title %}Editar usuario{% endblock %}

{% block body %}
    <h1>Editar usuario</h1>

    <div class = "card shadow col-lg-6">
        <div class = "card-body d-flex flex-column justify-content-start">

                {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
            <div class = "d-flex flex-row">
                <button class="btn btn-success mt-2 mr-1 col-lg-3">{{ button_label|default('Guardar') }}</button>
                {{ include('user/_delete_form.html.twig') }}
            </div>


            <a class = "btn btn-dark col-lg-6 mt-2" href="{{ path('app_user_index') }}"><i class = "fa fa-arrow-left"></i> Volver al índice</a>
        </div>
    </div>
{% endblock %}